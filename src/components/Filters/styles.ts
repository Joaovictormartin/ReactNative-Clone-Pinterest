import { StyleSheet } from "react-native";

import { theme } from "@/theme";

export const styles = StyleSheet.create({
  list: {
    paddingBottom: 4,
    maxHeight: 32,
  },
  content: {
    gap: 20,
    paddingHorizontal: 8,
  },
});
